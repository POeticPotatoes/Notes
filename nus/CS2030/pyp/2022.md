C
D
D
A
public interface Swappable \<S, T> {
    public abstract S doSwap(T t);
}

class Swap<S extends R, T extends R, R> implements Swappable<S, T> {}; //<>

Object
yes, given points a=(1, 2) b=(2, 3) and c=(3, 4), a.equals(b) \cong b.equals(c) \cong true but a.equals(c) \cong false, which violates the transitive property (ie point cannot be used in place of its parent violating lsp)

yes shouldd be compared in point2d

compile time 1 is raw value not class (i guess it's fine if it can tell)
complains during compile bridging method has cast to string
complains during runtime 1 is raw value
compiles and runs with warning

? extends T
? super T
T

Object String
String Object

static <T> void foo(List<? extends T> l1, List<? extends T> l2) {
}

List<Integer>
List<Double>
<>

can never overload super function meaningfully, because it will always be used over more specific overloads, thus type unsafe


